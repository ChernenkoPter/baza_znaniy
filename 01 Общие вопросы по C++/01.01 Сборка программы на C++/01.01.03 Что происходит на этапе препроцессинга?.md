Препроцессор исполняет директивы препроцессора - стандартные (if, ifdef, include, ...) и определенные пользователем через define

Все файлы с include при подстановке в единицу трансляции рекурсивно обрабатываются препроцессором