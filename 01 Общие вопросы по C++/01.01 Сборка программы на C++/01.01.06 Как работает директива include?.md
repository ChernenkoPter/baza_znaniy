Копирует все содержимое подключаемого файла. 
- `#incude <header>` - поиск в путях, заданных через `-I`, потом в стандартных директориях (например, `/usr/include`, `/usr/local/include`)
- `#include "header.h"` - сперва поиск в текущей директории, потом как у `<header.h>`
