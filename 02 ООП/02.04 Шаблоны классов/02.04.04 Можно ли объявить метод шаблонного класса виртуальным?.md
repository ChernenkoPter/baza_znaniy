Можно, если метод нешаблонный, но нельзя "добавлять" шаблонность: 
```
template <class A> struct X { template <class B> virtual void foo(B){} }; // ошибка времени компиляции
template <class A> struct Y {                    virtual void foo(){}  }; // OK
```