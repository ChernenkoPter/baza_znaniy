Resouse aquisition is initialization - идиома связывает время жизни ресурса и владеющего им объекта. Ресурс захватывается в конструкторе, освобождается в деструкторе. В контексте RAII ресурс — это всё, что требует явного освобождения (память, файлы, мьютексы, сокеты, соединения с БД и т.д.)
```
class File {
    FILE* f;
    File(const char* n) : f(fopen(n)) {} // захват
    ~File() { if(f) fclose(f);} // освобождение ресура
};
std::unique_ptr<int> up(new int(10));  // память
std::lock_guard<std::mutex> lock(mtx); // мьютекс
std::ifstream file("lol");             // файл
std::vector<int> vec;                  
```