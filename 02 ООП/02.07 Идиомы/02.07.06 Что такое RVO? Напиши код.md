Return Value Optimization - оптимизация компилятора, устраняющзая создание временного объекта при возврате из функции и копирование/перемещение возвращаемого значения
```
struct S {};
S f() { return {}; }
S s = f(); // объект создаётся сразу в s
```