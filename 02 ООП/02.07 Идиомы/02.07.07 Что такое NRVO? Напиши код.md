Named Return Value Optimization - оптимизация компилятора, устраняющая копирование/перемещение при возврате из функциии именованного локального объекта
```
struct S {};
S f() { S s_0; return s_0; }
S s_1 = f(); // объект создаётся сразу в s_1
```