Empty Base Optimization - пустой базовый класс (без нестатических полей, в том числе неявных как vtable от виртуальных функций или виртуального наследования) не занимает места в производном классе
```
struct Empty {};
struct Derived : Empty { int x; };

// sizeof(Derived) равно sizeof(int) из-за EBO
```