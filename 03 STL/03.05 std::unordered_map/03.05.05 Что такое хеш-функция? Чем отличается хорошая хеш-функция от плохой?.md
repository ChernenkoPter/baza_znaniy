Хэш функция в контексте unordered_map - функциональный объект с детерминированным `size_t operator()(const T&) const noexcept`. 

Хорошая хэш функция работает быстро и дает равномерное распределение для того множества ключей, с которым реально будет работать программа.
