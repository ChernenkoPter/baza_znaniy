Без учета выравнивания:
- `sizeof(unique_ptr<T, Deleter>) == sizeof(T*)`, если deleter оптимизирован с EBO:
    - std::default_delete
    - лямбда без захвата
    - не final структура без полей
- `sizeof(unique_ptr<T, Deleter>) == sizeof(T*) + sizeof(Deleter)` в остальных случаях
